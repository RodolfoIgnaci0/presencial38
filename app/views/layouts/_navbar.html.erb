<% if user_signed_in? %>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <a class="nav-link" href="#">Bucket List del 18º <span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">0 / 0 tareas (pendiente de programar)</a>
        </li>
      </ul>
    <div class=" my-2 my-lg-0">
        <%= link_to "Salir", destroy_user_session_path(), method: :delete, class: 'nav-link disabled' %>
    </div>
   </div>
  </nav>
<% end %>
